{% extends "@App/base.html.twig" %}

{% block content %}
<h1>Blog</h1>
<div>
    {% for article in articles %}
        <h2>{{ article.title }}</h2>
        <p>{{ article.body }}</p>
        <p><a href="/blog/{{ article.id }}">lien</a></p>
    {% endfor %}
</div>
{% endblock %}